[project]
name = "garmin"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "bullet",
    "flask",
    "garminconnect",
    "Jinja2",
    "psycopg2-binary",
    "sqlmodel",
]

[dependency-groups]
dev = [
    "beautifulsoup4",
    "black",
    "freezegun",
    "isort",
    "pytest",
    "pytest-cov",
    "ruff",
]

[tool.black]
# Pin target to the interpreter we run under; an open `requires-python = ">=3.14"`
# otherwise makes black target 3.15 and fail its AST safety check on 3.14.
target-version = ["py314"]

[tool.isort]
# Match black's import wrapping so the two don't reformat each other's output.
profile = "black"

[tool.pytest.ini_options]
# src/ is the import root, so tests import modules (analytics, db, ...) directly.
pythonpath = ["src"]
testpaths = ["tests"]